# LCF Civic Summaries API Configuration
# Cloudflare tunnel - HTTPS enabled (required for mixed content policy)
VITE_API_URL=https://watches-yen-bridal-connect.trycloudflare.com

# Note: HTTPS required because Lovable runs on HTTPS (Mixed Content Policy)